//! `demo normalize` — refine a raw macro into a clean demo score.

use crate::cli::NormalizeArgs;
use crate::error::{Error, Result};

pub fn run(_args: NormalizeArgs) -> Result<()> {
    Err(Error::Unimplemented("normalize"))
}
